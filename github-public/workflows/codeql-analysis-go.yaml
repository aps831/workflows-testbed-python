---
name: "CodeQL - Go"
on:
  workflow_dispatch:
  push:
    branches:
      - "master"
  pull_request:
    branches:
      - "master"
  schedule:
    - cron: "27 4 * * 5"
permissions:
  actions: read
  contents: read
  security-events: write
  issues: read
  pull-requests: read
jobs:
  analyze:
    name: codeql
    uses: aps831/workflows/.github/workflows/codeql-analysis-go.yaml@master
